[
<% @buildings.each_with_index do |building, index| %>
 {
  "building": {
   "id": <%= building.id %>,
   "name": "<%= building.name %>",
   "description": "<%= building.description %>",
   "polygon": "<%= building.encoded_polygon %>" 
  }
 <% if(index != @buildings.count-1) %>
 },
 <% else %>
 }
 <% end %>
<% end %>
]
